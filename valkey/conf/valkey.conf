# Minimal Valkey configuration for self-valkey container

# Network
bind 0.0.0.0
port 6379
protected-mode yes

# Persistence
dir /data
dbfilename dump.rdb
save 900 1
save 300 10
save 60 10000

# Keep defaults for most settings; override here only as needed.


maxclients 10000

timeout 1800